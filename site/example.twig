<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>{{ example.name }}</title>
  <script src="{{ publicPath }}example-common.js"></script>
</head>

<body>

<!-- example content -->
{% for file in example.files if file.type == 'html' %}
  {% for asset in file.assets if asset.type == 'html' %}
    {{ asset.source | raw }}
  {% endfor %}
{% endfor %}

<!-- example assets -->
{% for file in example.files %}
  {% for asset in file.assets %}
    {% if asset.type == 'css' %}
      <link rel="stylesheet" type="text/css" href="{{ publicPath }}{{ asset.path }}" />
    {% elseif asset.type == 'js' %}
      <script src="{{ publicPath }}{{ asset.path }}"></script>
    {% endif %}
  {% endfor %}
{% endfor %}

</body>
</html>
