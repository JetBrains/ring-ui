'use strict';

describe('Diff.DoubleEditorController', function () {
  var Controller = require('./double');

  it('should export module', function () {
    expect(new Controller()).toBeDefined();
  });
});
