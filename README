Ring UI: development environment setup

1. Install node.js

2: Install bower:

    npm install bower -g

3. Install dependencies:

    npm install && bower install

4. Start the watcher:

    grunt watch

4. Start the server:

    grunt connect

