Ring UI: development environment setup

1. Install node.js

2: Install grunt:

    npm install grunt -g

3. Install dependencies:

    npm install && grunt install

4. Start the watcher:

    grunt watch

4. Start the server:

    grunt connect

